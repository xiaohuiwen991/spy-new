/**
 * @Created by xxx
 * @Create date 2016.06.16
 */

function tt(){
    //name第一个名字是最后编辑者,第二个名字是创建者
    //alert('gulp comment --file comment.js --name EvanYao,ZhangMinJia');

    //一般创建者是不会变的,所以只写最后编辑者即可
    alert('gulp comment --file comment.js --name EvanYao');

    //如下可以给创建者编辑者使用同样的名字(一般并不用到,因为创建者第一次写死了不会变了)
    //alert('gulp comment --file comment.js --name EvanYao,');
}

/**
 * @Edited by xxx
 * @date
 */
